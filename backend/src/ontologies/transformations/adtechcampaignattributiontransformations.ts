/**
 * dbt SQL & Vectorized Transformation Model Registry: AdTechCampaignAttributionTransformations
 * Domain Focus: Multi-touch attribution (MTA), Shapley value attribution weights, and cost-per-acquisition (CPA) aggregation
 */

export interface TransformationModelSpec {
  transformId: string;
  modelName: string;
  category: string;
  sourceTables: string[];
  targetMartTable: string;
  materializationType: string;
  partitionColumn: string;
  clusterKeys: string[];
  preHookQueries: string[];
  postHookQueries: string[];
  sqlExpression: string;
}

export const TRANSFORMATION_ATC_DATASET: TransformationModelSpec[] = [
  {
    transformId: 'TRSF-ATC-001',
    modelName: 'AdTechCampaignAttributionTransformations Model v1',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_1', 'GRANT SELECT ON analytics_mart.atc_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-002',
    modelName: 'AdTechCampaignAttributionTransformations Model v2',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_2', 'GRANT SELECT ON analytics_mart.atc_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-003',
    modelName: 'AdTechCampaignAttributionTransformations Model v3',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_3', 'GRANT SELECT ON analytics_mart.atc_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-004',
    modelName: 'AdTechCampaignAttributionTransformations Model v4',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_4', 'GRANT SELECT ON analytics_mart.atc_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-005',
    modelName: 'AdTechCampaignAttributionTransformations Model v5',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_5', 'GRANT SELECT ON analytics_mart.atc_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-006',
    modelName: 'AdTechCampaignAttributionTransformations Model v6',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_6', 'GRANT SELECT ON analytics_mart.atc_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-007',
    modelName: 'AdTechCampaignAttributionTransformations Model v7',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_7', 'GRANT SELECT ON analytics_mart.atc_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-008',
    modelName: 'AdTechCampaignAttributionTransformations Model v8',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_8', 'GRANT SELECT ON analytics_mart.atc_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-009',
    modelName: 'AdTechCampaignAttributionTransformations Model v9',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_9', 'GRANT SELECT ON analytics_mart.atc_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-010',
    modelName: 'AdTechCampaignAttributionTransformations Model v10',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_10', 'GRANT SELECT ON analytics_mart.atc_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-011',
    modelName: 'AdTechCampaignAttributionTransformations Model v11',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_11', 'GRANT SELECT ON analytics_mart.atc_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-012',
    modelName: 'AdTechCampaignAttributionTransformations Model v12',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_12', 'GRANT SELECT ON analytics_mart.atc_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-013',
    modelName: 'AdTechCampaignAttributionTransformations Model v13',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_13', 'GRANT SELECT ON analytics_mart.atc_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-014',
    modelName: 'AdTechCampaignAttributionTransformations Model v14',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_14', 'GRANT SELECT ON analytics_mart.atc_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-015',
    modelName: 'AdTechCampaignAttributionTransformations Model v15',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_15', 'GRANT SELECT ON analytics_mart.atc_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-016',
    modelName: 'AdTechCampaignAttributionTransformations Model v16',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_16', 'GRANT SELECT ON analytics_mart.atc_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-017',
    modelName: 'AdTechCampaignAttributionTransformations Model v17',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_17', 'GRANT SELECT ON analytics_mart.atc_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-018',
    modelName: 'AdTechCampaignAttributionTransformations Model v18',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_18', 'GRANT SELECT ON analytics_mart.atc_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-019',
    modelName: 'AdTechCampaignAttributionTransformations Model v19',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_19', 'GRANT SELECT ON analytics_mart.atc_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-020',
    modelName: 'AdTechCampaignAttributionTransformations Model v20',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_20', 'GRANT SELECT ON analytics_mart.atc_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-021',
    modelName: 'AdTechCampaignAttributionTransformations Model v21',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_21', 'GRANT SELECT ON analytics_mart.atc_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-022',
    modelName: 'AdTechCampaignAttributionTransformations Model v22',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_22', 'GRANT SELECT ON analytics_mart.atc_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-023',
    modelName: 'AdTechCampaignAttributionTransformations Model v23',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_23', 'GRANT SELECT ON analytics_mart.atc_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-024',
    modelName: 'AdTechCampaignAttributionTransformations Model v24',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_24', 'GRANT SELECT ON analytics_mart.atc_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-025',
    modelName: 'AdTechCampaignAttributionTransformations Model v25',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_25', 'GRANT SELECT ON analytics_mart.atc_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-026',
    modelName: 'AdTechCampaignAttributionTransformations Model v26',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_26', 'GRANT SELECT ON analytics_mart.atc_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-027',
    modelName: 'AdTechCampaignAttributionTransformations Model v27',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_27', 'GRANT SELECT ON analytics_mart.atc_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-028',
    modelName: 'AdTechCampaignAttributionTransformations Model v28',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_28', 'GRANT SELECT ON analytics_mart.atc_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-029',
    modelName: 'AdTechCampaignAttributionTransformations Model v29',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_29', 'GRANT SELECT ON analytics_mart.atc_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-030',
    modelName: 'AdTechCampaignAttributionTransformations Model v30',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_0', 'GRANT SELECT ON analytics_mart.atc_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-031',
    modelName: 'AdTechCampaignAttributionTransformations Model v31',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_1', 'GRANT SELECT ON analytics_mart.atc_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-032',
    modelName: 'AdTechCampaignAttributionTransformations Model v32',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_2', 'GRANT SELECT ON analytics_mart.atc_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-033',
    modelName: 'AdTechCampaignAttributionTransformations Model v33',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_3', 'GRANT SELECT ON analytics_mart.atc_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-034',
    modelName: 'AdTechCampaignAttributionTransformations Model v34',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_4', 'GRANT SELECT ON analytics_mart.atc_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-035',
    modelName: 'AdTechCampaignAttributionTransformations Model v35',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_5', 'GRANT SELECT ON analytics_mart.atc_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-036',
    modelName: 'AdTechCampaignAttributionTransformations Model v36',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_6', 'GRANT SELECT ON analytics_mart.atc_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-037',
    modelName: 'AdTechCampaignAttributionTransformations Model v37',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_7', 'GRANT SELECT ON analytics_mart.atc_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-038',
    modelName: 'AdTechCampaignAttributionTransformations Model v38',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_8', 'GRANT SELECT ON analytics_mart.atc_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-039',
    modelName: 'AdTechCampaignAttributionTransformations Model v39',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_9', 'GRANT SELECT ON analytics_mart.atc_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-040',
    modelName: 'AdTechCampaignAttributionTransformations Model v40',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_10', 'GRANT SELECT ON analytics_mart.atc_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-041',
    modelName: 'AdTechCampaignAttributionTransformations Model v41',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_11', 'GRANT SELECT ON analytics_mart.atc_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-042',
    modelName: 'AdTechCampaignAttributionTransformations Model v42',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_12', 'GRANT SELECT ON analytics_mart.atc_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-043',
    modelName: 'AdTechCampaignAttributionTransformations Model v43',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_13', 'GRANT SELECT ON analytics_mart.atc_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-044',
    modelName: 'AdTechCampaignAttributionTransformations Model v44',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_14', 'GRANT SELECT ON analytics_mart.atc_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-045',
    modelName: 'AdTechCampaignAttributionTransformations Model v45',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_15', 'GRANT SELECT ON analytics_mart.atc_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-046',
    modelName: 'AdTechCampaignAttributionTransformations Model v46',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_16', 'GRANT SELECT ON analytics_mart.atc_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-047',
    modelName: 'AdTechCampaignAttributionTransformations Model v47',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_17', 'GRANT SELECT ON analytics_mart.atc_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-048',
    modelName: 'AdTechCampaignAttributionTransformations Model v48',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_18', 'GRANT SELECT ON analytics_mart.atc_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-049',
    modelName: 'AdTechCampaignAttributionTransformations Model v49',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_19', 'GRANT SELECT ON analytics_mart.atc_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-050',
    modelName: 'AdTechCampaignAttributionTransformations Model v50',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_20', 'GRANT SELECT ON analytics_mart.atc_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-051',
    modelName: 'AdTechCampaignAttributionTransformations Model v51',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_21', 'GRANT SELECT ON analytics_mart.atc_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-052',
    modelName: 'AdTechCampaignAttributionTransformations Model v52',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_22', 'GRANT SELECT ON analytics_mart.atc_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-053',
    modelName: 'AdTechCampaignAttributionTransformations Model v53',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_23', 'GRANT SELECT ON analytics_mart.atc_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-054',
    modelName: 'AdTechCampaignAttributionTransformations Model v54',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_24', 'GRANT SELECT ON analytics_mart.atc_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-055',
    modelName: 'AdTechCampaignAttributionTransformations Model v55',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_25', 'GRANT SELECT ON analytics_mart.atc_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-056',
    modelName: 'AdTechCampaignAttributionTransformations Model v56',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_26', 'GRANT SELECT ON analytics_mart.atc_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-057',
    modelName: 'AdTechCampaignAttributionTransformations Model v57',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_27', 'GRANT SELECT ON analytics_mart.atc_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-058',
    modelName: 'AdTechCampaignAttributionTransformations Model v58',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_28', 'GRANT SELECT ON analytics_mart.atc_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-059',
    modelName: 'AdTechCampaignAttributionTransformations Model v59',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_29', 'GRANT SELECT ON analytics_mart.atc_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-060',
    modelName: 'AdTechCampaignAttributionTransformations Model v60',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_0', 'GRANT SELECT ON analytics_mart.atc_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-061',
    modelName: 'AdTechCampaignAttributionTransformations Model v61',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_1', 'GRANT SELECT ON analytics_mart.atc_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-062',
    modelName: 'AdTechCampaignAttributionTransformations Model v62',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_2', 'GRANT SELECT ON analytics_mart.atc_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-063',
    modelName: 'AdTechCampaignAttributionTransformations Model v63',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_3', 'GRANT SELECT ON analytics_mart.atc_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-064',
    modelName: 'AdTechCampaignAttributionTransformations Model v64',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_4', 'GRANT SELECT ON analytics_mart.atc_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-065',
    modelName: 'AdTechCampaignAttributionTransformations Model v65',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_5', 'GRANT SELECT ON analytics_mart.atc_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-066',
    modelName: 'AdTechCampaignAttributionTransformations Model v66',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_6', 'GRANT SELECT ON analytics_mart.atc_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-067',
    modelName: 'AdTechCampaignAttributionTransformations Model v67',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_7', 'GRANT SELECT ON analytics_mart.atc_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-068',
    modelName: 'AdTechCampaignAttributionTransformations Model v68',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_8', 'GRANT SELECT ON analytics_mart.atc_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-069',
    modelName: 'AdTechCampaignAttributionTransformations Model v69',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_9', 'GRANT SELECT ON analytics_mart.atc_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-070',
    modelName: 'AdTechCampaignAttributionTransformations Model v70',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_10', 'GRANT SELECT ON analytics_mart.atc_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-071',
    modelName: 'AdTechCampaignAttributionTransformations Model v71',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_11', 'GRANT SELECT ON analytics_mart.atc_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-072',
    modelName: 'AdTechCampaignAttributionTransformations Model v72',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_12', 'GRANT SELECT ON analytics_mart.atc_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-073',
    modelName: 'AdTechCampaignAttributionTransformations Model v73',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_13', 'GRANT SELECT ON analytics_mart.atc_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-074',
    modelName: 'AdTechCampaignAttributionTransformations Model v74',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_14', 'GRANT SELECT ON analytics_mart.atc_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-075',
    modelName: 'AdTechCampaignAttributionTransformations Model v75',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_15', 'GRANT SELECT ON analytics_mart.atc_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-076',
    modelName: 'AdTechCampaignAttributionTransformations Model v76',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_16', 'GRANT SELECT ON analytics_mart.atc_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-077',
    modelName: 'AdTechCampaignAttributionTransformations Model v77',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_17', 'GRANT SELECT ON analytics_mart.atc_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-078',
    modelName: 'AdTechCampaignAttributionTransformations Model v78',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_18', 'GRANT SELECT ON analytics_mart.atc_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-079',
    modelName: 'AdTechCampaignAttributionTransformations Model v79',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_19', 'GRANT SELECT ON analytics_mart.atc_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-080',
    modelName: 'AdTechCampaignAttributionTransformations Model v80',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_20', 'GRANT SELECT ON analytics_mart.atc_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-081',
    modelName: 'AdTechCampaignAttributionTransformations Model v81',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_21', 'GRANT SELECT ON analytics_mart.atc_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-082',
    modelName: 'AdTechCampaignAttributionTransformations Model v82',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_22', 'GRANT SELECT ON analytics_mart.atc_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-083',
    modelName: 'AdTechCampaignAttributionTransformations Model v83',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_23', 'GRANT SELECT ON analytics_mart.atc_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-084',
    modelName: 'AdTechCampaignAttributionTransformations Model v84',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_24', 'GRANT SELECT ON analytics_mart.atc_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-085',
    modelName: 'AdTechCampaignAttributionTransformations Model v85',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_25', 'GRANT SELECT ON analytics_mart.atc_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-086',
    modelName: 'AdTechCampaignAttributionTransformations Model v86',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_26', 'GRANT SELECT ON analytics_mart.atc_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-087',
    modelName: 'AdTechCampaignAttributionTransformations Model v87',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_27', 'GRANT SELECT ON analytics_mart.atc_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-088',
    modelName: 'AdTechCampaignAttributionTransformations Model v88',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_28', 'GRANT SELECT ON analytics_mart.atc_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-089',
    modelName: 'AdTechCampaignAttributionTransformations Model v89',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_29', 'GRANT SELECT ON analytics_mart.atc_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-090',
    modelName: 'AdTechCampaignAttributionTransformations Model v90',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_0', 'GRANT SELECT ON analytics_mart.atc_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-091',
    modelName: 'AdTechCampaignAttributionTransformations Model v91',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_1', 'GRANT SELECT ON analytics_mart.atc_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-092',
    modelName: 'AdTechCampaignAttributionTransformations Model v92',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_2', 'GRANT SELECT ON analytics_mart.atc_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-093',
    modelName: 'AdTechCampaignAttributionTransformations Model v93',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_3', 'GRANT SELECT ON analytics_mart.atc_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-094',
    modelName: 'AdTechCampaignAttributionTransformations Model v94',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_4', 'GRANT SELECT ON analytics_mart.atc_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-095',
    modelName: 'AdTechCampaignAttributionTransformations Model v95',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_5', 'GRANT SELECT ON analytics_mart.atc_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-096',
    modelName: 'AdTechCampaignAttributionTransformations Model v96',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_6', 'GRANT SELECT ON analytics_mart.atc_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-097',
    modelName: 'AdTechCampaignAttributionTransformations Model v97',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_7', 'GRANT SELECT ON analytics_mart.atc_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-098',
    modelName: 'AdTechCampaignAttributionTransformations Model v98',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_8', 'GRANT SELECT ON analytics_mart.atc_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-099',
    modelName: 'AdTechCampaignAttributionTransformations Model v99',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_9', 'GRANT SELECT ON analytics_mart.atc_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-100',
    modelName: 'AdTechCampaignAttributionTransformations Model v100',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_10', 'GRANT SELECT ON analytics_mart.atc_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-101',
    modelName: 'AdTechCampaignAttributionTransformations Model v101',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_11', 'GRANT SELECT ON analytics_mart.atc_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-102',
    modelName: 'AdTechCampaignAttributionTransformations Model v102',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_12', 'GRANT SELECT ON analytics_mart.atc_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-103',
    modelName: 'AdTechCampaignAttributionTransformations Model v103',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_13', 'GRANT SELECT ON analytics_mart.atc_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-104',
    modelName: 'AdTechCampaignAttributionTransformations Model v104',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_14', 'GRANT SELECT ON analytics_mart.atc_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-105',
    modelName: 'AdTechCampaignAttributionTransformations Model v105',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_15', 'GRANT SELECT ON analytics_mart.atc_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-106',
    modelName: 'AdTechCampaignAttributionTransformations Model v106',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_16', 'GRANT SELECT ON analytics_mart.atc_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-107',
    modelName: 'AdTechCampaignAttributionTransformations Model v107',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_17', 'GRANT SELECT ON analytics_mart.atc_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-108',
    modelName: 'AdTechCampaignAttributionTransformations Model v108',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_18', 'GRANT SELECT ON analytics_mart.atc_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-109',
    modelName: 'AdTechCampaignAttributionTransformations Model v109',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_19', 'GRANT SELECT ON analytics_mart.atc_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-110',
    modelName: 'AdTechCampaignAttributionTransformations Model v110',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_20', 'GRANT SELECT ON analytics_mart.atc_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-111',
    modelName: 'AdTechCampaignAttributionTransformations Model v111',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_21', 'GRANT SELECT ON analytics_mart.atc_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-112',
    modelName: 'AdTechCampaignAttributionTransformations Model v112',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_22', 'GRANT SELECT ON analytics_mart.atc_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-113',
    modelName: 'AdTechCampaignAttributionTransformations Model v113',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_23', 'GRANT SELECT ON analytics_mart.atc_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-114',
    modelName: 'AdTechCampaignAttributionTransformations Model v114',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_24', 'GRANT SELECT ON analytics_mart.atc_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-115',
    modelName: 'AdTechCampaignAttributionTransformations Model v115',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_25', 'GRANT SELECT ON analytics_mart.atc_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-116',
    modelName: 'AdTechCampaignAttributionTransformations Model v116',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_26', 'GRANT SELECT ON analytics_mart.atc_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-117',
    modelName: 'AdTechCampaignAttributionTransformations Model v117',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_27', 'GRANT SELECT ON analytics_mart.atc_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-118',
    modelName: 'AdTechCampaignAttributionTransformations Model v118',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_28', 'GRANT SELECT ON analytics_mart.atc_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-119',
    modelName: 'AdTechCampaignAttributionTransformations Model v119',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_29', 'GRANT SELECT ON analytics_mart.atc_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-120',
    modelName: 'AdTechCampaignAttributionTransformations Model v120',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_0', 'GRANT SELECT ON analytics_mart.atc_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-121',
    modelName: 'AdTechCampaignAttributionTransformations Model v121',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_1', 'GRANT SELECT ON analytics_mart.atc_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-122',
    modelName: 'AdTechCampaignAttributionTransformations Model v122',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_2', 'GRANT SELECT ON analytics_mart.atc_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-123',
    modelName: 'AdTechCampaignAttributionTransformations Model v123',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_3', 'GRANT SELECT ON analytics_mart.atc_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-124',
    modelName: 'AdTechCampaignAttributionTransformations Model v124',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_4', 'GRANT SELECT ON analytics_mart.atc_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-125',
    modelName: 'AdTechCampaignAttributionTransformations Model v125',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_5', 'GRANT SELECT ON analytics_mart.atc_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-126',
    modelName: 'AdTechCampaignAttributionTransformations Model v126',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_6', 'GRANT SELECT ON analytics_mart.atc_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-127',
    modelName: 'AdTechCampaignAttributionTransformations Model v127',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_7', 'GRANT SELECT ON analytics_mart.atc_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-128',
    modelName: 'AdTechCampaignAttributionTransformations Model v128',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_8', 'GRANT SELECT ON analytics_mart.atc_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-129',
    modelName: 'AdTechCampaignAttributionTransformations Model v129',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_9', 'GRANT SELECT ON analytics_mart.atc_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-130',
    modelName: 'AdTechCampaignAttributionTransformations Model v130',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_10', 'GRANT SELECT ON analytics_mart.atc_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-131',
    modelName: 'AdTechCampaignAttributionTransformations Model v131',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_11', 'GRANT SELECT ON analytics_mart.atc_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-132',
    modelName: 'AdTechCampaignAttributionTransformations Model v132',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_12', 'GRANT SELECT ON analytics_mart.atc_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-133',
    modelName: 'AdTechCampaignAttributionTransformations Model v133',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_13', 'GRANT SELECT ON analytics_mart.atc_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-134',
    modelName: 'AdTechCampaignAttributionTransformations Model v134',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_14', 'GRANT SELECT ON analytics_mart.atc_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-135',
    modelName: 'AdTechCampaignAttributionTransformations Model v135',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_15', 'GRANT SELECT ON analytics_mart.atc_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-136',
    modelName: 'AdTechCampaignAttributionTransformations Model v136',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_16', 'GRANT SELECT ON analytics_mart.atc_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-137',
    modelName: 'AdTechCampaignAttributionTransformations Model v137',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_17', 'GRANT SELECT ON analytics_mart.atc_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-138',
    modelName: 'AdTechCampaignAttributionTransformations Model v138',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_18', 'GRANT SELECT ON analytics_mart.atc_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-139',
    modelName: 'AdTechCampaignAttributionTransformations Model v139',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_19', 'GRANT SELECT ON analytics_mart.atc_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-140',
    modelName: 'AdTechCampaignAttributionTransformations Model v140',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_20', 'GRANT SELECT ON analytics_mart.atc_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-141',
    modelName: 'AdTechCampaignAttributionTransformations Model v141',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_21', 'GRANT SELECT ON analytics_mart.atc_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-142',
    modelName: 'AdTechCampaignAttributionTransformations Model v142',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_22', 'GRANT SELECT ON analytics_mart.atc_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-143',
    modelName: 'AdTechCampaignAttributionTransformations Model v143',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_23', 'GRANT SELECT ON analytics_mart.atc_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-144',
    modelName: 'AdTechCampaignAttributionTransformations Model v144',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_24', 'GRANT SELECT ON analytics_mart.atc_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-145',
    modelName: 'AdTechCampaignAttributionTransformations Model v145',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_25', 'GRANT SELECT ON analytics_mart.atc_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-146',
    modelName: 'AdTechCampaignAttributionTransformations Model v146',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_26', 'GRANT SELECT ON analytics_mart.atc_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-147',
    modelName: 'AdTechCampaignAttributionTransformations Model v147',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_27', 'GRANT SELECT ON analytics_mart.atc_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-148',
    modelName: 'AdTechCampaignAttributionTransformations Model v148',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_28', 'GRANT SELECT ON analytics_mart.atc_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-149',
    modelName: 'AdTechCampaignAttributionTransformations Model v149',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_29', 'GRANT SELECT ON analytics_mart.atc_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-150',
    modelName: 'AdTechCampaignAttributionTransformations Model v150',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_0', 'GRANT SELECT ON analytics_mart.atc_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-151',
    modelName: 'AdTechCampaignAttributionTransformations Model v151',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_1', 'GRANT SELECT ON analytics_mart.atc_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-152',
    modelName: 'AdTechCampaignAttributionTransformations Model v152',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_2', 'GRANT SELECT ON analytics_mart.atc_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-153',
    modelName: 'AdTechCampaignAttributionTransformations Model v153',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_3', 'GRANT SELECT ON analytics_mart.atc_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-154',
    modelName: 'AdTechCampaignAttributionTransformations Model v154',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_4', 'GRANT SELECT ON analytics_mart.atc_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-155',
    modelName: 'AdTechCampaignAttributionTransformations Model v155',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_5', 'GRANT SELECT ON analytics_mart.atc_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-156',
    modelName: 'AdTechCampaignAttributionTransformations Model v156',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_6', 'GRANT SELECT ON analytics_mart.atc_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-157',
    modelName: 'AdTechCampaignAttributionTransformations Model v157',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_7', 'GRANT SELECT ON analytics_mart.atc_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-158',
    modelName: 'AdTechCampaignAttributionTransformations Model v158',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_8', 'GRANT SELECT ON analytics_mart.atc_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-159',
    modelName: 'AdTechCampaignAttributionTransformations Model v159',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_9', 'GRANT SELECT ON analytics_mart.atc_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-160',
    modelName: 'AdTechCampaignAttributionTransformations Model v160',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_10', 'GRANT SELECT ON analytics_mart.atc_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-161',
    modelName: 'AdTechCampaignAttributionTransformations Model v161',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_11', 'GRANT SELECT ON analytics_mart.atc_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-162',
    modelName: 'AdTechCampaignAttributionTransformations Model v162',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_12', 'GRANT SELECT ON analytics_mart.atc_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-163',
    modelName: 'AdTechCampaignAttributionTransformations Model v163',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_13', 'GRANT SELECT ON analytics_mart.atc_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-164',
    modelName: 'AdTechCampaignAttributionTransformations Model v164',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_14', 'GRANT SELECT ON analytics_mart.atc_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-165',
    modelName: 'AdTechCampaignAttributionTransformations Model v165',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_15', 'GRANT SELECT ON analytics_mart.atc_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-166',
    modelName: 'AdTechCampaignAttributionTransformations Model v166',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_16', 'GRANT SELECT ON analytics_mart.atc_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-167',
    modelName: 'AdTechCampaignAttributionTransformations Model v167',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_17', 'GRANT SELECT ON analytics_mart.atc_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-168',
    modelName: 'AdTechCampaignAttributionTransformations Model v168',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_18', 'GRANT SELECT ON analytics_mart.atc_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-169',
    modelName: 'AdTechCampaignAttributionTransformations Model v169',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_19', 'GRANT SELECT ON analytics_mart.atc_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-170',
    modelName: 'AdTechCampaignAttributionTransformations Model v170',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_20', 'GRANT SELECT ON analytics_mart.atc_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-171',
    modelName: 'AdTechCampaignAttributionTransformations Model v171',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_21', 'GRANT SELECT ON analytics_mart.atc_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-172',
    modelName: 'AdTechCampaignAttributionTransformations Model v172',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_22', 'GRANT SELECT ON analytics_mart.atc_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-173',
    modelName: 'AdTechCampaignAttributionTransformations Model v173',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_23', 'GRANT SELECT ON analytics_mart.atc_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-174',
    modelName: 'AdTechCampaignAttributionTransformations Model v174',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_24', 'GRANT SELECT ON analytics_mart.atc_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-175',
    modelName: 'AdTechCampaignAttributionTransformations Model v175',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_25', 'GRANT SELECT ON analytics_mart.atc_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-176',
    modelName: 'AdTechCampaignAttributionTransformations Model v176',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_26', 'GRANT SELECT ON analytics_mart.atc_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-177',
    modelName: 'AdTechCampaignAttributionTransformations Model v177',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_27', 'GRANT SELECT ON analytics_mart.atc_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-178',
    modelName: 'AdTechCampaignAttributionTransformations Model v178',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_28', 'GRANT SELECT ON analytics_mart.atc_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-179',
    modelName: 'AdTechCampaignAttributionTransformations Model v179',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_29', 'GRANT SELECT ON analytics_mart.atc_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-180',
    modelName: 'AdTechCampaignAttributionTransformations Model v180',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_0', 'GRANT SELECT ON analytics_mart.atc_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-181',
    modelName: 'AdTechCampaignAttributionTransformations Model v181',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_1', 'GRANT SELECT ON analytics_mart.atc_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-182',
    modelName: 'AdTechCampaignAttributionTransformations Model v182',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_2', 'GRANT SELECT ON analytics_mart.atc_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-183',
    modelName: 'AdTechCampaignAttributionTransformations Model v183',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_3', 'GRANT SELECT ON analytics_mart.atc_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-184',
    modelName: 'AdTechCampaignAttributionTransformations Model v184',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_4', 'GRANT SELECT ON analytics_mart.atc_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-185',
    modelName: 'AdTechCampaignAttributionTransformations Model v185',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_5', 'GRANT SELECT ON analytics_mart.atc_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-186',
    modelName: 'AdTechCampaignAttributionTransformations Model v186',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.atc_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_6', 'GRANT SELECT ON analytics_mart.atc_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-187',
    modelName: 'AdTechCampaignAttributionTransformations Model v187',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.atc_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_7', 'GRANT SELECT ON analytics_mart.atc_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-188',
    modelName: 'AdTechCampaignAttributionTransformations Model v188',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.atc_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_8', 'GRANT SELECT ON analytics_mart.atc_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-189',
    modelName: 'AdTechCampaignAttributionTransformations Model v189',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.atc_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_9', 'GRANT SELECT ON analytics_mart.atc_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-190',
    modelName: 'AdTechCampaignAttributionTransformations Model v190',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.atc_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_10', 'GRANT SELECT ON analytics_mart.atc_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-191',
    modelName: 'AdTechCampaignAttributionTransformations Model v191',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.atc_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_11', 'GRANT SELECT ON analytics_mart.atc_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-192',
    modelName: 'AdTechCampaignAttributionTransformations Model v192',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.atc_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_12', 'GRANT SELECT ON analytics_mart.atc_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-193',
    modelName: 'AdTechCampaignAttributionTransformations Model v193',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.atc_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_13', 'GRANT SELECT ON analytics_mart.atc_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-194',
    modelName: 'AdTechCampaignAttributionTransformations Model v194',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.atc_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_14', 'GRANT SELECT ON analytics_mart.atc_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-195',
    modelName: 'AdTechCampaignAttributionTransformations Model v195',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.atc_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_15', 'GRANT SELECT ON analytics_mart.atc_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-196',
    modelName: 'AdTechCampaignAttributionTransformations Model v196',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.atc_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_16', 'GRANT SELECT ON analytics_mart.atc_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-197',
    modelName: 'AdTechCampaignAttributionTransformations Model v197',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.atc_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_17', 'GRANT SELECT ON analytics_mart.atc_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-198',
    modelName: 'AdTechCampaignAttributionTransformations Model v198',
    category: 'Multi-touch attribution (MTA)',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.atc_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_18', 'GRANT SELECT ON analytics_mart.atc_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-199',
    modelName: 'AdTechCampaignAttributionTransformations Model v199',
    category: 'Shapley value attribution weights',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.atc_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_19', 'GRANT SELECT ON analytics_mart.atc_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-ATC-200',
    modelName: 'AdTechCampaignAttributionTransformations Model v200',
    category: 'and cost-per-acquisition (CPA) aggregation',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.atc_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.atc_fact_20', 'GRANT SELECT ON analytics_mart.atc_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  }
];
