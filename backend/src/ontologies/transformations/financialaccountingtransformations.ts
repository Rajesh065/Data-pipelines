/**
 * dbt SQL & Vectorized Transformation Model Registry: FinancialAccountingTransformations
 * Domain Focus: Balance sheet adjustments, general ledger rollup, multi-currency conversion, and tax withholding computations
 */

export interface TransformationModelSpec {
  transformId: string;
  modelName: string;
  category: string;
  sourceTables: string[];
  targetMartTable: string;
  materializationType: string;
  partitionColumn: string;
  clusterKeys: string[];
  preHookQueries: string[];
  postHookQueries: string[];
  sqlExpression: string;
}

export const TRANSFORMATION_FAT_DATASET: TransformationModelSpec[] = [
  {
    transformId: 'TRSF-FAT-001',
    modelName: 'FinancialAccountingTransformations Model v1',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_1', 'GRANT SELECT ON analytics_mart.fat_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-002',
    modelName: 'FinancialAccountingTransformations Model v2',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_2', 'GRANT SELECT ON analytics_mart.fat_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-003',
    modelName: 'FinancialAccountingTransformations Model v3',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_3', 'GRANT SELECT ON analytics_mart.fat_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-004',
    modelName: 'FinancialAccountingTransformations Model v4',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_4', 'GRANT SELECT ON analytics_mart.fat_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-005',
    modelName: 'FinancialAccountingTransformations Model v5',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_5', 'GRANT SELECT ON analytics_mart.fat_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-006',
    modelName: 'FinancialAccountingTransformations Model v6',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_6', 'GRANT SELECT ON analytics_mart.fat_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-007',
    modelName: 'FinancialAccountingTransformations Model v7',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_7', 'GRANT SELECT ON analytics_mart.fat_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-008',
    modelName: 'FinancialAccountingTransformations Model v8',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_8', 'GRANT SELECT ON analytics_mart.fat_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-009',
    modelName: 'FinancialAccountingTransformations Model v9',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_9', 'GRANT SELECT ON analytics_mart.fat_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-010',
    modelName: 'FinancialAccountingTransformations Model v10',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_10', 'GRANT SELECT ON analytics_mart.fat_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-011',
    modelName: 'FinancialAccountingTransformations Model v11',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_11', 'GRANT SELECT ON analytics_mart.fat_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-012',
    modelName: 'FinancialAccountingTransformations Model v12',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_12', 'GRANT SELECT ON analytics_mart.fat_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-013',
    modelName: 'FinancialAccountingTransformations Model v13',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_13', 'GRANT SELECT ON analytics_mart.fat_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-014',
    modelName: 'FinancialAccountingTransformations Model v14',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_14', 'GRANT SELECT ON analytics_mart.fat_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-015',
    modelName: 'FinancialAccountingTransformations Model v15',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_15', 'GRANT SELECT ON analytics_mart.fat_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-016',
    modelName: 'FinancialAccountingTransformations Model v16',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_16', 'GRANT SELECT ON analytics_mart.fat_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-017',
    modelName: 'FinancialAccountingTransformations Model v17',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_17', 'GRANT SELECT ON analytics_mart.fat_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-018',
    modelName: 'FinancialAccountingTransformations Model v18',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_18', 'GRANT SELECT ON analytics_mart.fat_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-019',
    modelName: 'FinancialAccountingTransformations Model v19',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_19', 'GRANT SELECT ON analytics_mart.fat_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-020',
    modelName: 'FinancialAccountingTransformations Model v20',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_20', 'GRANT SELECT ON analytics_mart.fat_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-021',
    modelName: 'FinancialAccountingTransformations Model v21',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_21', 'GRANT SELECT ON analytics_mart.fat_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-022',
    modelName: 'FinancialAccountingTransformations Model v22',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_22', 'GRANT SELECT ON analytics_mart.fat_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-023',
    modelName: 'FinancialAccountingTransformations Model v23',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_23', 'GRANT SELECT ON analytics_mart.fat_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-024',
    modelName: 'FinancialAccountingTransformations Model v24',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_24', 'GRANT SELECT ON analytics_mart.fat_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-025',
    modelName: 'FinancialAccountingTransformations Model v25',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_25', 'GRANT SELECT ON analytics_mart.fat_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-026',
    modelName: 'FinancialAccountingTransformations Model v26',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_26', 'GRANT SELECT ON analytics_mart.fat_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-027',
    modelName: 'FinancialAccountingTransformations Model v27',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_27', 'GRANT SELECT ON analytics_mart.fat_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-028',
    modelName: 'FinancialAccountingTransformations Model v28',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_28', 'GRANT SELECT ON analytics_mart.fat_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-029',
    modelName: 'FinancialAccountingTransformations Model v29',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_29', 'GRANT SELECT ON analytics_mart.fat_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-030',
    modelName: 'FinancialAccountingTransformations Model v30',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_0', 'GRANT SELECT ON analytics_mart.fat_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-031',
    modelName: 'FinancialAccountingTransformations Model v31',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_1', 'GRANT SELECT ON analytics_mart.fat_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-032',
    modelName: 'FinancialAccountingTransformations Model v32',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_2', 'GRANT SELECT ON analytics_mart.fat_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-033',
    modelName: 'FinancialAccountingTransformations Model v33',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_3', 'GRANT SELECT ON analytics_mart.fat_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-034',
    modelName: 'FinancialAccountingTransformations Model v34',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_4', 'GRANT SELECT ON analytics_mart.fat_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-035',
    modelName: 'FinancialAccountingTransformations Model v35',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_5', 'GRANT SELECT ON analytics_mart.fat_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-036',
    modelName: 'FinancialAccountingTransformations Model v36',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_6', 'GRANT SELECT ON analytics_mart.fat_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-037',
    modelName: 'FinancialAccountingTransformations Model v37',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_7', 'GRANT SELECT ON analytics_mart.fat_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-038',
    modelName: 'FinancialAccountingTransformations Model v38',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_8', 'GRANT SELECT ON analytics_mart.fat_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-039',
    modelName: 'FinancialAccountingTransformations Model v39',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_9', 'GRANT SELECT ON analytics_mart.fat_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-040',
    modelName: 'FinancialAccountingTransformations Model v40',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_10', 'GRANT SELECT ON analytics_mart.fat_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-041',
    modelName: 'FinancialAccountingTransformations Model v41',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_11', 'GRANT SELECT ON analytics_mart.fat_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-042',
    modelName: 'FinancialAccountingTransformations Model v42',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_12', 'GRANT SELECT ON analytics_mart.fat_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-043',
    modelName: 'FinancialAccountingTransformations Model v43',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_13', 'GRANT SELECT ON analytics_mart.fat_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-044',
    modelName: 'FinancialAccountingTransformations Model v44',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_14', 'GRANT SELECT ON analytics_mart.fat_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-045',
    modelName: 'FinancialAccountingTransformations Model v45',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_15', 'GRANT SELECT ON analytics_mart.fat_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-046',
    modelName: 'FinancialAccountingTransformations Model v46',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_16', 'GRANT SELECT ON analytics_mart.fat_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-047',
    modelName: 'FinancialAccountingTransformations Model v47',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_17', 'GRANT SELECT ON analytics_mart.fat_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-048',
    modelName: 'FinancialAccountingTransformations Model v48',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_18', 'GRANT SELECT ON analytics_mart.fat_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-049',
    modelName: 'FinancialAccountingTransformations Model v49',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_19', 'GRANT SELECT ON analytics_mart.fat_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-050',
    modelName: 'FinancialAccountingTransformations Model v50',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_20', 'GRANT SELECT ON analytics_mart.fat_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-051',
    modelName: 'FinancialAccountingTransformations Model v51',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_21', 'GRANT SELECT ON analytics_mart.fat_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-052',
    modelName: 'FinancialAccountingTransformations Model v52',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_22', 'GRANT SELECT ON analytics_mart.fat_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-053',
    modelName: 'FinancialAccountingTransformations Model v53',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_23', 'GRANT SELECT ON analytics_mart.fat_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-054',
    modelName: 'FinancialAccountingTransformations Model v54',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_24', 'GRANT SELECT ON analytics_mart.fat_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-055',
    modelName: 'FinancialAccountingTransformations Model v55',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_25', 'GRANT SELECT ON analytics_mart.fat_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-056',
    modelName: 'FinancialAccountingTransformations Model v56',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_26', 'GRANT SELECT ON analytics_mart.fat_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-057',
    modelName: 'FinancialAccountingTransformations Model v57',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_27', 'GRANT SELECT ON analytics_mart.fat_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-058',
    modelName: 'FinancialAccountingTransformations Model v58',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_28', 'GRANT SELECT ON analytics_mart.fat_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-059',
    modelName: 'FinancialAccountingTransformations Model v59',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_29', 'GRANT SELECT ON analytics_mart.fat_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-060',
    modelName: 'FinancialAccountingTransformations Model v60',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_0', 'GRANT SELECT ON analytics_mart.fat_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-061',
    modelName: 'FinancialAccountingTransformations Model v61',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_1', 'GRANT SELECT ON analytics_mart.fat_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-062',
    modelName: 'FinancialAccountingTransformations Model v62',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_2', 'GRANT SELECT ON analytics_mart.fat_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-063',
    modelName: 'FinancialAccountingTransformations Model v63',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_3', 'GRANT SELECT ON analytics_mart.fat_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-064',
    modelName: 'FinancialAccountingTransformations Model v64',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_4', 'GRANT SELECT ON analytics_mart.fat_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-065',
    modelName: 'FinancialAccountingTransformations Model v65',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_5', 'GRANT SELECT ON analytics_mart.fat_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-066',
    modelName: 'FinancialAccountingTransformations Model v66',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_6', 'GRANT SELECT ON analytics_mart.fat_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-067',
    modelName: 'FinancialAccountingTransformations Model v67',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_7', 'GRANT SELECT ON analytics_mart.fat_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-068',
    modelName: 'FinancialAccountingTransformations Model v68',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_8', 'GRANT SELECT ON analytics_mart.fat_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-069',
    modelName: 'FinancialAccountingTransformations Model v69',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_9', 'GRANT SELECT ON analytics_mart.fat_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-070',
    modelName: 'FinancialAccountingTransformations Model v70',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_10', 'GRANT SELECT ON analytics_mart.fat_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-071',
    modelName: 'FinancialAccountingTransformations Model v71',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_11', 'GRANT SELECT ON analytics_mart.fat_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-072',
    modelName: 'FinancialAccountingTransformations Model v72',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_12', 'GRANT SELECT ON analytics_mart.fat_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-073',
    modelName: 'FinancialAccountingTransformations Model v73',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_13', 'GRANT SELECT ON analytics_mart.fat_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-074',
    modelName: 'FinancialAccountingTransformations Model v74',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_14', 'GRANT SELECT ON analytics_mart.fat_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-075',
    modelName: 'FinancialAccountingTransformations Model v75',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_15', 'GRANT SELECT ON analytics_mart.fat_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-076',
    modelName: 'FinancialAccountingTransformations Model v76',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_16', 'GRANT SELECT ON analytics_mart.fat_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-077',
    modelName: 'FinancialAccountingTransformations Model v77',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_17', 'GRANT SELECT ON analytics_mart.fat_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-078',
    modelName: 'FinancialAccountingTransformations Model v78',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_18', 'GRANT SELECT ON analytics_mart.fat_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-079',
    modelName: 'FinancialAccountingTransformations Model v79',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_19', 'GRANT SELECT ON analytics_mart.fat_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-080',
    modelName: 'FinancialAccountingTransformations Model v80',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_20', 'GRANT SELECT ON analytics_mart.fat_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-081',
    modelName: 'FinancialAccountingTransformations Model v81',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_21', 'GRANT SELECT ON analytics_mart.fat_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-082',
    modelName: 'FinancialAccountingTransformations Model v82',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_22', 'GRANT SELECT ON analytics_mart.fat_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-083',
    modelName: 'FinancialAccountingTransformations Model v83',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_23', 'GRANT SELECT ON analytics_mart.fat_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-084',
    modelName: 'FinancialAccountingTransformations Model v84',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_24', 'GRANT SELECT ON analytics_mart.fat_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-085',
    modelName: 'FinancialAccountingTransformations Model v85',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_25', 'GRANT SELECT ON analytics_mart.fat_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-086',
    modelName: 'FinancialAccountingTransformations Model v86',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_26', 'GRANT SELECT ON analytics_mart.fat_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-087',
    modelName: 'FinancialAccountingTransformations Model v87',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_27', 'GRANT SELECT ON analytics_mart.fat_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-088',
    modelName: 'FinancialAccountingTransformations Model v88',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_28', 'GRANT SELECT ON analytics_mart.fat_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-089',
    modelName: 'FinancialAccountingTransformations Model v89',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_29', 'GRANT SELECT ON analytics_mart.fat_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-090',
    modelName: 'FinancialAccountingTransformations Model v90',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_0', 'GRANT SELECT ON analytics_mart.fat_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-091',
    modelName: 'FinancialAccountingTransformations Model v91',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_1', 'GRANT SELECT ON analytics_mart.fat_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-092',
    modelName: 'FinancialAccountingTransformations Model v92',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_2', 'GRANT SELECT ON analytics_mart.fat_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-093',
    modelName: 'FinancialAccountingTransformations Model v93',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_3', 'GRANT SELECT ON analytics_mart.fat_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-094',
    modelName: 'FinancialAccountingTransformations Model v94',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_4', 'GRANT SELECT ON analytics_mart.fat_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-095',
    modelName: 'FinancialAccountingTransformations Model v95',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_5', 'GRANT SELECT ON analytics_mart.fat_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-096',
    modelName: 'FinancialAccountingTransformations Model v96',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_6', 'GRANT SELECT ON analytics_mart.fat_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-097',
    modelName: 'FinancialAccountingTransformations Model v97',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_7', 'GRANT SELECT ON analytics_mart.fat_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-098',
    modelName: 'FinancialAccountingTransformations Model v98',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_8', 'GRANT SELECT ON analytics_mart.fat_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-099',
    modelName: 'FinancialAccountingTransformations Model v99',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_9', 'GRANT SELECT ON analytics_mart.fat_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-100',
    modelName: 'FinancialAccountingTransformations Model v100',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_10', 'GRANT SELECT ON analytics_mart.fat_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-101',
    modelName: 'FinancialAccountingTransformations Model v101',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_11', 'GRANT SELECT ON analytics_mart.fat_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-102',
    modelName: 'FinancialAccountingTransformations Model v102',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_12', 'GRANT SELECT ON analytics_mart.fat_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-103',
    modelName: 'FinancialAccountingTransformations Model v103',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_13', 'GRANT SELECT ON analytics_mart.fat_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-104',
    modelName: 'FinancialAccountingTransformations Model v104',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_14', 'GRANT SELECT ON analytics_mart.fat_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-105',
    modelName: 'FinancialAccountingTransformations Model v105',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_15', 'GRANT SELECT ON analytics_mart.fat_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-106',
    modelName: 'FinancialAccountingTransformations Model v106',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_16', 'GRANT SELECT ON analytics_mart.fat_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-107',
    modelName: 'FinancialAccountingTransformations Model v107',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_17', 'GRANT SELECT ON analytics_mart.fat_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-108',
    modelName: 'FinancialAccountingTransformations Model v108',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_18', 'GRANT SELECT ON analytics_mart.fat_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-109',
    modelName: 'FinancialAccountingTransformations Model v109',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_19', 'GRANT SELECT ON analytics_mart.fat_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-110',
    modelName: 'FinancialAccountingTransformations Model v110',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_20', 'GRANT SELECT ON analytics_mart.fat_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-111',
    modelName: 'FinancialAccountingTransformations Model v111',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_21', 'GRANT SELECT ON analytics_mart.fat_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-112',
    modelName: 'FinancialAccountingTransformations Model v112',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_22', 'GRANT SELECT ON analytics_mart.fat_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-113',
    modelName: 'FinancialAccountingTransformations Model v113',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_23', 'GRANT SELECT ON analytics_mart.fat_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-114',
    modelName: 'FinancialAccountingTransformations Model v114',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_24', 'GRANT SELECT ON analytics_mart.fat_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-115',
    modelName: 'FinancialAccountingTransformations Model v115',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_25', 'GRANT SELECT ON analytics_mart.fat_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-116',
    modelName: 'FinancialAccountingTransformations Model v116',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_26', 'GRANT SELECT ON analytics_mart.fat_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-117',
    modelName: 'FinancialAccountingTransformations Model v117',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_27', 'GRANT SELECT ON analytics_mart.fat_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-118',
    modelName: 'FinancialAccountingTransformations Model v118',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_28', 'GRANT SELECT ON analytics_mart.fat_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-119',
    modelName: 'FinancialAccountingTransformations Model v119',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_29', 'GRANT SELECT ON analytics_mart.fat_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-120',
    modelName: 'FinancialAccountingTransformations Model v120',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_0', 'GRANT SELECT ON analytics_mart.fat_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-121',
    modelName: 'FinancialAccountingTransformations Model v121',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_1', 'GRANT SELECT ON analytics_mart.fat_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-122',
    modelName: 'FinancialAccountingTransformations Model v122',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_2', 'GRANT SELECT ON analytics_mart.fat_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-123',
    modelName: 'FinancialAccountingTransformations Model v123',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_3', 'GRANT SELECT ON analytics_mart.fat_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-124',
    modelName: 'FinancialAccountingTransformations Model v124',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_4', 'GRANT SELECT ON analytics_mart.fat_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-125',
    modelName: 'FinancialAccountingTransformations Model v125',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_5', 'GRANT SELECT ON analytics_mart.fat_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-126',
    modelName: 'FinancialAccountingTransformations Model v126',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_6', 'GRANT SELECT ON analytics_mart.fat_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-127',
    modelName: 'FinancialAccountingTransformations Model v127',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_7', 'GRANT SELECT ON analytics_mart.fat_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-128',
    modelName: 'FinancialAccountingTransformations Model v128',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_8', 'GRANT SELECT ON analytics_mart.fat_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-129',
    modelName: 'FinancialAccountingTransformations Model v129',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_9', 'GRANT SELECT ON analytics_mart.fat_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-130',
    modelName: 'FinancialAccountingTransformations Model v130',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_10', 'GRANT SELECT ON analytics_mart.fat_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-131',
    modelName: 'FinancialAccountingTransformations Model v131',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_11', 'GRANT SELECT ON analytics_mart.fat_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-132',
    modelName: 'FinancialAccountingTransformations Model v132',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_12', 'GRANT SELECT ON analytics_mart.fat_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-133',
    modelName: 'FinancialAccountingTransformations Model v133',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_13', 'GRANT SELECT ON analytics_mart.fat_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-134',
    modelName: 'FinancialAccountingTransformations Model v134',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_14', 'GRANT SELECT ON analytics_mart.fat_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-135',
    modelName: 'FinancialAccountingTransformations Model v135',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_15', 'GRANT SELECT ON analytics_mart.fat_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-136',
    modelName: 'FinancialAccountingTransformations Model v136',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_16', 'GRANT SELECT ON analytics_mart.fat_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-137',
    modelName: 'FinancialAccountingTransformations Model v137',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_17', 'GRANT SELECT ON analytics_mart.fat_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-138',
    modelName: 'FinancialAccountingTransformations Model v138',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_18', 'GRANT SELECT ON analytics_mart.fat_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-139',
    modelName: 'FinancialAccountingTransformations Model v139',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_19', 'GRANT SELECT ON analytics_mart.fat_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-140',
    modelName: 'FinancialAccountingTransformations Model v140',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_20', 'GRANT SELECT ON analytics_mart.fat_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-141',
    modelName: 'FinancialAccountingTransformations Model v141',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_21', 'GRANT SELECT ON analytics_mart.fat_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-142',
    modelName: 'FinancialAccountingTransformations Model v142',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_22', 'GRANT SELECT ON analytics_mart.fat_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-143',
    modelName: 'FinancialAccountingTransformations Model v143',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_23', 'GRANT SELECT ON analytics_mart.fat_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-144',
    modelName: 'FinancialAccountingTransformations Model v144',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_24', 'GRANT SELECT ON analytics_mart.fat_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-145',
    modelName: 'FinancialAccountingTransformations Model v145',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_25', 'GRANT SELECT ON analytics_mart.fat_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-146',
    modelName: 'FinancialAccountingTransformations Model v146',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_26', 'GRANT SELECT ON analytics_mart.fat_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-147',
    modelName: 'FinancialAccountingTransformations Model v147',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_27', 'GRANT SELECT ON analytics_mart.fat_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-148',
    modelName: 'FinancialAccountingTransformations Model v148',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_28', 'GRANT SELECT ON analytics_mart.fat_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-149',
    modelName: 'FinancialAccountingTransformations Model v149',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_29', 'GRANT SELECT ON analytics_mart.fat_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-150',
    modelName: 'FinancialAccountingTransformations Model v150',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_0', 'GRANT SELECT ON analytics_mart.fat_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-151',
    modelName: 'FinancialAccountingTransformations Model v151',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_1', 'GRANT SELECT ON analytics_mart.fat_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-152',
    modelName: 'FinancialAccountingTransformations Model v152',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_2', 'GRANT SELECT ON analytics_mart.fat_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-153',
    modelName: 'FinancialAccountingTransformations Model v153',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_3', 'GRANT SELECT ON analytics_mart.fat_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-154',
    modelName: 'FinancialAccountingTransformations Model v154',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_4', 'GRANT SELECT ON analytics_mart.fat_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-155',
    modelName: 'FinancialAccountingTransformations Model v155',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_5', 'GRANT SELECT ON analytics_mart.fat_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-156',
    modelName: 'FinancialAccountingTransformations Model v156',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_6', 'GRANT SELECT ON analytics_mart.fat_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-157',
    modelName: 'FinancialAccountingTransformations Model v157',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_7', 'GRANT SELECT ON analytics_mart.fat_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-158',
    modelName: 'FinancialAccountingTransformations Model v158',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_8', 'GRANT SELECT ON analytics_mart.fat_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-159',
    modelName: 'FinancialAccountingTransformations Model v159',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_9', 'GRANT SELECT ON analytics_mart.fat_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-160',
    modelName: 'FinancialAccountingTransformations Model v160',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_10', 'GRANT SELECT ON analytics_mart.fat_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-161',
    modelName: 'FinancialAccountingTransformations Model v161',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_11', 'GRANT SELECT ON analytics_mart.fat_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-162',
    modelName: 'FinancialAccountingTransformations Model v162',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_12', 'GRANT SELECT ON analytics_mart.fat_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-163',
    modelName: 'FinancialAccountingTransformations Model v163',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_13', 'GRANT SELECT ON analytics_mart.fat_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-164',
    modelName: 'FinancialAccountingTransformations Model v164',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_14', 'GRANT SELECT ON analytics_mart.fat_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-165',
    modelName: 'FinancialAccountingTransformations Model v165',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_15', 'GRANT SELECT ON analytics_mart.fat_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-166',
    modelName: 'FinancialAccountingTransformations Model v166',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_16', 'GRANT SELECT ON analytics_mart.fat_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-167',
    modelName: 'FinancialAccountingTransformations Model v167',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_17', 'GRANT SELECT ON analytics_mart.fat_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-168',
    modelName: 'FinancialAccountingTransformations Model v168',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_18', 'GRANT SELECT ON analytics_mart.fat_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-169',
    modelName: 'FinancialAccountingTransformations Model v169',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_19', 'GRANT SELECT ON analytics_mart.fat_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-170',
    modelName: 'FinancialAccountingTransformations Model v170',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_20', 'GRANT SELECT ON analytics_mart.fat_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-171',
    modelName: 'FinancialAccountingTransformations Model v171',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_21',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_21', 'GRANT SELECT ON analytics_mart.fat_fact_21 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-172',
    modelName: 'FinancialAccountingTransformations Model v172',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_22',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_22', 'GRANT SELECT ON analytics_mart.fat_fact_22 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-173',
    modelName: 'FinancialAccountingTransformations Model v173',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_23',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_23', 'GRANT SELECT ON analytics_mart.fat_fact_23 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-174',
    modelName: 'FinancialAccountingTransformations Model v174',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_24',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_24', 'GRANT SELECT ON analytics_mart.fat_fact_24 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-175',
    modelName: 'FinancialAccountingTransformations Model v175',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_25',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_25', 'GRANT SELECT ON analytics_mart.fat_fact_25 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-176',
    modelName: 'FinancialAccountingTransformations Model v176',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_1', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_26',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_26', 'GRANT SELECT ON analytics_mart.fat_fact_26 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-177',
    modelName: 'FinancialAccountingTransformations Model v177',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_2', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_27',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_27', 'GRANT SELECT ON analytics_mart.fat_fact_27 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-178',
    modelName: 'FinancialAccountingTransformations Model v178',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_3', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_28',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_28', 'GRANT SELECT ON analytics_mart.fat_fact_28 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-179',
    modelName: 'FinancialAccountingTransformations Model v179',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_4', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_29',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_29', 'GRANT SELECT ON analytics_mart.fat_fact_29 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-180',
    modelName: 'FinancialAccountingTransformations Model v180',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_5', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_0',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_0', 'GRANT SELECT ON analytics_mart.fat_fact_0 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-181',
    modelName: 'FinancialAccountingTransformations Model v181',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_6', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_1',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_1', 'GRANT SELECT ON analytics_mart.fat_fact_1 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-182',
    modelName: 'FinancialAccountingTransformations Model v182',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_7', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_2',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_2', 'GRANT SELECT ON analytics_mart.fat_fact_2 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-183',
    modelName: 'FinancialAccountingTransformations Model v183',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_8', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_3',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_3', 'GRANT SELECT ON analytics_mart.fat_fact_3 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-184',
    modelName: 'FinancialAccountingTransformations Model v184',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_9', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_4',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_4', 'GRANT SELECT ON analytics_mart.fat_fact_4 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-185',
    modelName: 'FinancialAccountingTransformations Model v185',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_10', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_5',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_5', 'GRANT SELECT ON analytics_mart.fat_fact_5 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-186',
    modelName: 'FinancialAccountingTransformations Model v186',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_11', 'dim_lookup.catalog_6'],
    targetMartTable: 'analytics_mart.fat_fact_6',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_6', 'GRANT SELECT ON analytics_mart.fat_fact_6 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-187',
    modelName: 'FinancialAccountingTransformations Model v187',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_12', 'dim_lookup.catalog_7'],
    targetMartTable: 'analytics_mart.fat_fact_7',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_7', 'GRANT SELECT ON analytics_mart.fat_fact_7 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-188',
    modelName: 'FinancialAccountingTransformations Model v188',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_13', 'dim_lookup.catalog_8'],
    targetMartTable: 'analytics_mart.fat_fact_8',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_8', 'GRANT SELECT ON analytics_mart.fat_fact_8 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-189',
    modelName: 'FinancialAccountingTransformations Model v189',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_14', 'dim_lookup.catalog_9'],
    targetMartTable: 'analytics_mart.fat_fact_9',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_9', 'GRANT SELECT ON analytics_mart.fat_fact_9 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-190',
    modelName: 'FinancialAccountingTransformations Model v190',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_15', 'dim_lookup.catalog_10'],
    targetMartTable: 'analytics_mart.fat_fact_10',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_10', 'GRANT SELECT ON analytics_mart.fat_fact_10 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-191',
    modelName: 'FinancialAccountingTransformations Model v191',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_16', 'dim_lookup.catalog_11'],
    targetMartTable: 'analytics_mart.fat_fact_11',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_11', 'GRANT SELECT ON analytics_mart.fat_fact_11 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-192',
    modelName: 'FinancialAccountingTransformations Model v192',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_17', 'dim_lookup.catalog_12'],
    targetMartTable: 'analytics_mart.fat_fact_12',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_12', 'GRANT SELECT ON analytics_mart.fat_fact_12 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-193',
    modelName: 'FinancialAccountingTransformations Model v193',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_18', 'dim_lookup.catalog_13'],
    targetMartTable: 'analytics_mart.fat_fact_13',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_13', 'GRANT SELECT ON analytics_mart.fat_fact_13 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-194',
    modelName: 'FinancialAccountingTransformations Model v194',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_19', 'dim_lookup.catalog_14'],
    targetMartTable: 'analytics_mart.fat_fact_14',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_14', 'GRANT SELECT ON analytics_mart.fat_fact_14 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-195',
    modelName: 'FinancialAccountingTransformations Model v195',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_20', 'dim_lookup.catalog_0'],
    targetMartTable: 'analytics_mart.fat_fact_15',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_15', 'GRANT SELECT ON analytics_mart.fat_fact_15 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-196',
    modelName: 'FinancialAccountingTransformations Model v196',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_21', 'dim_lookup.catalog_1'],
    targetMartTable: 'analytics_mart.fat_fact_16',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_16', 'GRANT SELECT ON analytics_mart.fat_fact_16 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-197',
    modelName: 'FinancialAccountingTransformations Model v197',
    category: 'general ledger rollup',
    sourceTables: ['raw_staging.table_22', 'dim_lookup.catalog_2'],
    targetMartTable: 'analytics_mart.fat_fact_17',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_17', 'GRANT SELECT ON analytics_mart.fat_fact_17 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-198',
    modelName: 'FinancialAccountingTransformations Model v198',
    category: 'multi-currency conversion',
    sourceTables: ['raw_staging.table_23', 'dim_lookup.catalog_3'],
    targetMartTable: 'analytics_mart.fat_fact_18',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_18', 'GRANT SELECT ON analytics_mart.fat_fact_18 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-199',
    modelName: 'FinancialAccountingTransformations Model v199',
    category: 'and tax withholding computations',
    sourceTables: ['raw_staging.table_24', 'dim_lookup.catalog_4'],
    targetMartTable: 'analytics_mart.fat_fact_19',
    materializationType: 'TABLE',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_19', 'GRANT SELECT ON analytics_mart.fat_fact_19 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  },
  {
    transformId: 'TRSF-FAT-200',
    modelName: 'FinancialAccountingTransformations Model v200',
    category: 'Balance sheet adjustments',
    sourceTables: ['raw_staging.table_0', 'dim_lookup.catalog_5'],
    targetMartTable: 'analytics_mart.fat_fact_20',
    materializationType: 'INCREMENTAL',
    partitionColumn: 'event_date',
    clusterKeys: ['tenant_id', 'entity_id'],
    preHookQueries: ['SET timezone = "UTC"', 'CREATE TEMPORARY TABLE IF NOT EXISTS _tmp_batch'],
    postHookQueries: ['ANALYZE analytics_mart.fat_fact_20', 'GRANT SELECT ON analytics_mart.fat_fact_20 TO ROLE bi_reporting'],
    sqlExpression: 'SELECT record_id, entity_id, SUM(metric_value) OVER(PARTITION BY entity_id ORDER BY event_timestamp ROWS BETWEEN 30 PRECEDING AND CURRENT ROW) AS rolling_30d_metric FROM source_dataset WHERE is_valid = TRUE'
  }
];
